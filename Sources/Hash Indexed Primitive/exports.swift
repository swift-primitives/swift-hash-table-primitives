// exports.swift
// Hash Indexed Primitive declares `Hash.Indexed<Dense>` (the ordered hashed COLUMN:
// dense insertion-ordered elements + the bucket position-index engine) + `Hash.Key`
// (the single-point key bound). Per the exports-narrowing ruling (audit #9), nothing
// is re-exported: consumers import the column vocabulary explicitly.
