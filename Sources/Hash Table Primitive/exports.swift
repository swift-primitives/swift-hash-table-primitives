// exports.swift — Hash Table Primitive (the position-index engine).
// Declares Hash.Table (tombstone-free backward-shift, per-instance seed) extending the
// `Hash` namespace owned by swift-hash-primitives. Per the exports-narrowing ruling
// (audit #9), nothing is re-exported: consumers import the vocabulary
// (Hash/Index/Buffer.Slots/…) explicitly.
