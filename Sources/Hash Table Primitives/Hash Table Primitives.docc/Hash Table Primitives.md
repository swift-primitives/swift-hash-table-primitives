# ``Hash_Table_Primitives``

@Metadata {
    @DisplayName("Hash Table Primitives")
    @TitleHeading("Swift Primitives")
}

An open-addressed hash table mapping elements to their typed `Index<Element>` positions in external storage, with O(1) average lookup — the index layer that hashed collections build on.

## Topics
